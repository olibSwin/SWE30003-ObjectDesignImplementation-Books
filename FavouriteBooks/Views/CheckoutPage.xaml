<Page x:Class="FavouriteBooks.Views.CheckoutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FavouriteBooks.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CheckoutPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Content="Back" Click="Back_Click" Width="50" HorizontalAlignment="Left" Margin="25,0,0,0" Height="25"/>
        <TextBlock Grid.ColumnSpan="2" Text="Checkout" FontSize="30" FontFamily="Arial Black" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <StackPanel Grid.Row="1" Margin="150,70,0,0">
            <TextBlock Grid.Row="1" Text="Customer Details" FontSize="20" TextDecorations="Underline" FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal" Margin="10,5,0,5">
                <TextBlock Text="Name:" TextDecorations="Underline" />
                <TextBlock x:Name="CustomerName" Margin="10,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,0,0,5">
                <TextBlock Text="Email:" TextDecorations="Underline" />
                <TextBlock x:Name="CustomerEmail" Margin="10,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,0,0,5">
                <TextBlock Text="Phone Number:" TextDecorations="Underline" />
                <TextBlock x:Name="CustomerPhone" Margin="10,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,0,0,10">
                <TextBlock Text="Deliver Address:" TextDecorations="Underline" />
                <TextBlock x:Name="CustomerAddr" Margin="10,0,0,0" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="50,70,0,0">
            <TextBlock Grid.Row="1" Text="Order Details" FontSize="20" TextDecorations="Underline" FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal" Margin="10,5,0,5">
                <TextBlock Text="Number of Items:" TextDecorations="Underline" />
                <TextBlock x:Name="OrderItemCount" Margin="10,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,0,0,5">
                <TextBlock Text="Subtotal:" TextDecorations="Underline" />
                <TextBlock x:Name="OrderSubtotal" Margin="10,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,0,0,5">
                <TextBlock Text="Shipping Cost:" TextDecorations="Underline" />
                <TextBlock x:Name="OrderShippingCost" Margin="10,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,0,0,5">
                <TextBlock Text="Total (incl. shipping):" TextDecorations="Underline" />
                <TextBlock x:Name="OrderTotalCost" Margin="10,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5,0,0,0">
                <Label Margin="0,0,10,0" Target="{Binding ElementName=paymentMethodDropdown}">Payment Type:</Label>
                <ComboBox Width="200" Height="25" HorizontalAlignment="Left" Name="PaymentMethodDropdown" Text="Select Payment Type" />
            </StackPanel>
        </StackPanel>

        <Button Grid.Row="2" Grid.ColumnSpan="2" Margin="0,70,0,0" Content="Pay" Click="Pay_Click" Width="150" Height="35" />
    </Grid>
</Page>
