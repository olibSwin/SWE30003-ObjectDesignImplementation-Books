<Page x:Class="FavouriteBooks.Views.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FavouriteBooks.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CartPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Button Content="Back" Click="Back_Click" Width="50" HorizontalAlignment="Left" Margin="25,0,0,0" Height="25"/>
        <TextBlock Text="Cart" FontSize="30" FontFamily="Arial Black" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <StackPanel Margin="10" Grid.Row="2" Grid.ColumnSpan="2">
            <DataGrid x:Name="CartItemGrid" Height="300" AutoGenerateColumns="False" CanUserResizeColumns="False" CanUserReorderColumns="False" MinColumnWidth="75" IsReadOnly="True" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Book.Title}" />
                    <DataGridTextColumn Header="Author" Binding="{Binding Book.Author}" />
                    <DataGridTextColumn Header="Price" Binding="{Binding Book.Price, StringFormat=C}" />
                    <DataGridTemplateColumn Header="Quantity">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Quantity}" MinWidth="25" TextAlignment="Center" VerticalAlignment="Center" />
                                    <Button Content="+" Click="IncreaseQuantity_Click" Width="20" Height="20" FontSize="12" />
                                    <Button Content="-" Click="DecreaseQuantity_Click" Width="20" Height="20" FontSize="12" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock x:Name="TotalText" FontSize="16" />

            <Button Content="Checkout" Click="Checkout_Click" Width="100" Height="25" />
        </StackPanel>
    </Grid>
</Page>
