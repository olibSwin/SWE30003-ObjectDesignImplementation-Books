<Page x:Class="FavouriteBooks.Views.BookCataloguePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FavouriteBooks.Views"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Book Catalogue">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top bar with search on left and cart on right -->
        <Grid Grid.Row="0" Margin="0 0 0 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search bar on the left -->
            <StackPanel Grid.Column="0" 
                        Orientation="Horizontal">
                <TextBox x:Name="SearchBox" 
                         Width="300" 
                         Margin="0 0 10 0"
                         Padding="5"/>
                <Button Content="Search" 
                        Click="Search_Click"
                        Padding="10 5"/>
            </StackPanel>

            <!-- Cart button on the right -->
            <Button Grid.Column="1"
                    Content="🛒 Cart"
                    Click="Cart_Click"
                    Padding="10 5"/>
        </Grid>

        <!-- Book list in a border with scrolling -->
        <Border Grid.Row="1" 
        BorderBrush="#2C3E50" 
        BorderThickness="1" 
        CornerRadius="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ListBox x:Name="BookList"
                 BorderThickness="0"
                 Padding="5"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>